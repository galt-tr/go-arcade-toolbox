name: Integration Tests

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - labeled
      - synchronize
  workflow_dispatch:

permissions:
  contents: read

jobs:
  integration:
    name: Integration tests
    # Runs on pushes to main, manual dispatch, or on PRs that carry the
    # "integration" label (add the label to opt a PR into this workflow;
    # new pushes to an already-labeled PR re-run it via `synchronize`).
    if: >-
      contains(github.event.pull_request.labels.*.name, 'integration') ||
      github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Verify Docker is available
        run: docker version

      - name: Run integration tests (race detector)
        # -race, not the plain target: every concurrency property this module
        # claims — SKIP LOCKED disjointness, concurrent funding, the reconciler's
        # exactly-once release — lives behind the integration tag, so the plain
        # `make test-race` in ci.yml never sees any of them.
        run: make test-integration-race
