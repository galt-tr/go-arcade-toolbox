// Package testusers provides a small set of fixed test users (with
// deterministic private keys) shared across internal tests that need a
// stable identity, key deriver, or address to sign or derive against.
package testusers
