// Package pending provides a repository for caching pending sign actions
// (transactions awaiting a follow-up call to complete signing) between the
// createAction and signAction steps of the wallet's action lifecycle.
package pending
